# Kotlin/JVM read-only bridge

Минимальный Kotlin-прототип читает компактный JSON из `smc-read --status-json` и рассчитывает уровень каждого вентилятора по его фактическим RPM и собственному диапазону. Единый уровень для значка — максимум из двух. Никаких операций записи в SMC нет.

```bash
make -C ../smc-read build
gradle --offline run --args=../smc-read/smc-read
```

Это проверка связки Kotlin/JVM → нативное чтение через локальный процесс. Для окончательного приложения потребуется решить, как включить нативный код в `.app`, как хранить и проверять путь к нему и какой IPC нужен привилегированному помощнику для будущей записи. Пока прототип только печатает один снимок в терминал.
